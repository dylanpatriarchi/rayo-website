export const metadata = {
    title: "Grazie per l'iscrizione",
    description: "Sei iscritto alla newsletter Rayo.",
    robots: { index: false, follow: true },
};

export default function ThankYouNewsletterLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}
